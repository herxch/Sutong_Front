// Active job postings. Empty array renders the "no openings" message.
// To post a job, push an object: { id, title, body: <JSX or string> }
export const JOBS = [];

export const HR_EMAIL = "hr@sutongctr.com";
